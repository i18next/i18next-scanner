import { Fragment } from 'react';

const ComponentDefVal = () => (
    <Fragment>
        <Trans>Trans component should respect the defaultValue option</Trans>
    </Fragment>
);

export default ComponentDefVal;
